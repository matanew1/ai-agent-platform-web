export const DEFAULT_PROMPT =
  "You are a careful research assistant. Answer with the retrieved context and tool results. If you do not have enough information, say so instead of guessing.";

export const DEFAULT_MODEL = "qwen3:8b";
export const DEFAULT_TEMPERATURE = 0.3;
